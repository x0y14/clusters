server {
    listen  80;
    server_name _;
    
    location /api/ {
        proxy_pass http://backend:8000/;
    }

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri /index.html;  # SPA の場合: ルーティングを index.html にフォールバック
    }
}